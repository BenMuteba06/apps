version: 1.0
runtime: python3
build:
  commands:
    pre-build:
      - python3 --version
      - pip3 --version
    build:
      - pip3 install -r requirements.txt
run:
  command: python3 -m streamlit run streamlit/app.py --server.port 8080 --server.address 0.0.0.0
  network:
    port: 8080